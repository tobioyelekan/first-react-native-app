class Product {
    constructor(id, ownerId, title, imageUrl, desc, price) {
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        this.imageUrl = imageUrl;
        this.desc = desc;
        this.price = price;
    }
}

export default Product;